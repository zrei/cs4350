using System.Collections.Generic;
using System.Linq;
using UnityEngine;

/*
public class FlagCondition : Condition
{
    [Tooltip("Flags must be true")]
    public List<Flags> m_Flags;

    public override bool Evaluate()
    {
        return m_Flags.All(x => FlagManager.Instance.GetFlagValue(x.ToString()));
    }
}
*/